local TileGrid
local disorder

if not TileGrid then TileGrid = require 'stp/TileGrid' end
if not disorder then disorder  = require 'stp/Disorder' end

--local TileGame = {}
--TileGame._mt = {__index = TileGame}

local initialize
local gameLoop
local run

initialize = function ()
end

gameLoop = function ()
end

run = function ()
end
